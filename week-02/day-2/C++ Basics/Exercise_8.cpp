#include <iostream>
#include <string>

using namespace std;

int main() {
  int h[] = {3, 4, 5, 6, 7};
  int length = sizeof(h) / sizeof(h[1]);
  int sum = 0;
  for (int i = 0; i < length; i++)
    sum = sum + e[i];

  cout << sum;
	// print the sum of all numbers in h

  return 0;
}
