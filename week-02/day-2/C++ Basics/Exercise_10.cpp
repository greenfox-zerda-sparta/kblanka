#include <iostream>
#include <string>

using namespace std;

void doubleit (int number) {
	int result = number * 2;
	cout << "The result is " << result << "." << endl;
}

int main() {
  int j = 123;
  doubleit (j);

  // create a function that doubles it's input
  // double j with it

  return 0;
}
